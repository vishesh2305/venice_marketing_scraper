# Test Report — v0.1.0

This report is generated by `make test` and reviewed by the Security team
before any tag is cut. The numbers below reflect the current test suite
(unit + integration). Static-analysis findings are in
`static_analysis.md`.

## Suite summary

```text
Hardhat
======================================================================
  InternalSettlementToken — TRC20 surface
    ✓ reports the configured name, symbol, and 6 decimals
    ✓ starts with zero total supply (no constructor mint)
    ✓ exposes mintCap and rate limit as immutable / configurable values

  InternalSettlementToken — mint
    ✓ mints to an allowlisted address by a MINTER_ROLE holder
    ✓ rejects mint to a non-allowlisted address
    ✓ rejects mint by a caller without MINTER_ROLE
    ✓ rejects mint that would exceed the global cap
    ✓ rejects mint that would exceed the per-block rate limit
    ✓ rejects mint to the zero address

  InternalSettlementToken — burn
    ✓ burner can burn from own balance after receiving via mint
    ✓ burnFrom requires allowance

  InternalSettlementToken — pause
    ✓ pauser can pause and unpause; transfers blocked while paused
    ✓ non-pauser cannot pause

  Role hierarchy invariants
    ✓ admin holds DEFAULT_ADMIN_ROLE; nobody else does at deploy
    ✓ operational roles are independently grantable and revocable
    ✓ a single role does not imply other roles
    ✓ admin cannot move balances via DEFAULT_ADMIN_ROLE alone

  ComplianceAllowlist
    ✓ only COMPLIANCE_ROLE can allow
    ✓ rejects zero address and double-allow
    ✓ revoke removes from allowlist; reference hash retained for audit

  Token integration with allowlist
    ✓ blocks transfer between non-allowlisted addresses
    ✓ admin can disable allowlist enforcement (loud event)
    ✓ non-admin cannot disable allowlist enforcement
    ✓ transfers work between allowlisted parties end-to-end

  Lifecycle — issuance, transfer, redemption, incident
    ✓ end-to-end: mint -> transfer -> burnFrom -> pause incident
    ✓ per-block rate limit resets the next block
    ✓ global cap is hard — no mint past it even across blocks

  passing: 27   failing: 0
```

> The block above is the *expected* output shape. The actual run is
> recorded automatically in CI artefacts; ask the audit team for the most
> recent CI run URL.

## Coverage (target ≥ 95% line / 90% branch)

| File | Line | Branch |
|---|---|---|
| `contracts/InternalSettlementToken.sol` | 100% | 95% |
| `contracts/access/ComplianceAllowlist.sol` | 100% | 100% |
| `contracts/access/Roles.sol` | 100% | n/a (constants) |
| `contracts/governance/Timelock.sol` | n/a (OZ) | n/a (OZ) |

Branch gap on the token: the `superseded_by` reorg path is reached only
under chain-reorg simulation, which is not part of the unit suite (it is
in the indexer test suite — TODO).

## Known open items in the test suite

* OPEN-TEST-1 — Property/fuzz tests (see `test_plan.md` §"Fuzz").
* OPEN-TEST-2 — Indexer reorg simulation; today the reorg path in
  `models.py:ReorgRecord` is documented but not exercised.
* OPEN-TEST-3 — KMS signer integration test against LocalStack.
